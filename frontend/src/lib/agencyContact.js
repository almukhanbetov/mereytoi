// Single source of truth for the agency's contact number, so it isn't
// hardcoded separately in every component that links to WhatsApp/tel:.
export const AGENCY_PHONE_DISPLAY = '+7 707 650 00 51';
export const AGENCY_WHATSAPP_DIGITS = '77076500051';
